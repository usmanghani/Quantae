@if(Request.IsAuthenticated) {
    <text>Welcome @User.Identity.Name !
    [ @Html.ActionLink("Log Off", "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink("Log On", "LogOn", "Account") ]
    @:[ @Html.ActionLink("Register", "Register", "Account") ]
}

